<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 自定义loading dialog -->
    <style name="loading_dialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


    <!--分割线-->
    <style name="bg_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/divider_line</item>
    </style>

    <!--分割线  百分之75透明度-->
    <style name="bg_line_75">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/transparent_75_white</item>
    </style>
    <!--竖直分割线-->
    <style name="bg_vertical_line">
        <item name="android:layout_width">0.5dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/transparent_75_white</item>
    </style>
    <!--微信全屏加透明-->
    <style name="thirdlibrary_TranslucentFullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="ads_dialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- 不带有遮罩层的 Dialog 效果 -->
    <style name="BottomNoShadeDialog">
        <!--<item name="android:windowBackground">@android:color/transparent</item>-->
        <!--<item name="android:windowFrame">@null</item>-->
        <!--<item name="android:windowNoTitle">true</item>-->
        <item name="android:windowIsFloating">true</item>
        <!--<item name="android:windowIsTranslucent">true</item>-->
        <!--<item name="android:windowContentOverlay">@null</item>-->
        <!-- Dialog show、dismiss 的动画 -->
        <item name="android:windowAnimationStyle">@style/DialogBottomSlideInOutAnim</item>
        <!-- 设置是否显示遮罩层 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- 带有遮罩层的Dialog效果 -->
    <style name="BottomShadeDialog" parent="@style/BottomNoShadeDialog">
        <!-- 设置是否显示遮罩层 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置遮罩层的灰度值 -->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <!-- 底部滑入滑出 动画-->
    <style name="DialogBottomSlideInOutAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_bottom_slide_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_bottom_slide_out</item>
    </style>

    <style name="SystemDialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Animation_Bottom_Dialog">
        <item name="android:windowEnterAnimation">@anim/bottom_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_dialog_exit</item>
    </style>
    <style name="verify_edittext">
        <item name="android:textSize">@dimen/sp_18</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/dp_8</item>
        <item name="android:background">@drawable/verification_edit_bg</item>
        <item name="android:inputType">phone</item>
        <item name="android:textColor">@color/color_C43413</item>
        <item name="android:maxLength">1</item>
        <item name="android:cursorVisible">true</item>
    </style>
</resources>
